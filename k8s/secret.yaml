# NETAI Chatbot — Secret (API keys)
# NOTE: In production, use Kubernetes Secrets management (e.g., Sealed Secrets, Vault)
apiVersion: v1
kind: Secret
metadata:
  name: netai-chatbot-secrets
  namespace: netai
  labels:
    app.kubernetes.io/name: netai-chatbot
type: Opaque
stringData:
  LLM_API_KEY: "replace-with-your-nrp-api-key"
  PERFSONAR_API_KEY: ""
